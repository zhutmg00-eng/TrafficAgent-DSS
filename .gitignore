# Byte-compiled / optimized / URL mapping files
__pycache__/
*.py[cod]
*$py.class

# Environments & Virtual Envs
.env
.venv
env/
venv/
ENV/
env.bak/
venv.bak/

# IDE / Editor files
.vscode/
.idea/
*.swp
*.swo

# Office temporary lock files
~$*.docx
~$*.doc
~$*.xlsx
~$*.pptx

# SUMO / Simulation outputs & temp logs
*.out.xml
*.tripinfo.xml
*.summary.xml
*.trajectories.xml
log/
logs/
*.log

# OS generated files
Thumbs.db
Desktop.ini
.DS_Store

# Local competition notices, internal drafts and submission templates (kept locally only)
# 说明：本组规则用于隔离「本地赛事材料」，避免内部通知/草稿进入公开仓库。
#      正式交付物（作品说明书、申报书等 PDF）请放入 docs/ 目录 —— 该目录下的 PDF 已开例外，
#      不会被下面的通配规则忽略，可正常提交。
附件*
*通知*
agent.md
docs/*_draft.md
docs/competition_guide.md
# 其余位置的 PDF 视为本地材料；docs/ 下的正式 PDF 例外放行
*.pdf
!docs/*.pdf

# Teamwork subagent runtime workspaces
.agents/
ORIGINAL_REQUEST.md
.temp_inspection/
*.zip

# ZCode 会话运行时目录（本地工具产物，不入库）
.zcode/

# 消融实验的原始逐种子数据（可由 experiments/ablation.py 重新生成）；
# 汇总用的 ablation_result_*.md 作为样例证据保留入库
experiments/ablation_raw_*.json
# 闭环增益实验的原始逐种子数据与逐次运行日志（可由 experiments/closed_loop_gain.py 重新生成）；
# 汇总用的 closed_loop_gain_*.md 保留入库
experiments/closed_loop_gain_raw_*.json

# Pytest & Test Artifacts
.pytest_cache/
tests/e2e/screenshots/


# UI contrast audit artifacts (local measurement output)
ui_audit_*.json
