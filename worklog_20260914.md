# TrafficAgent-DSS 环境搭建 + 审查 + 消融复跑 · 总览

- 日期：2026-09-14
- 项目位置：`E:\TrafficAgent-DSS`（HEAD `4d615a4` v2.2.1）；另有干净副本 `E:\TrafficAgent-DSS-clean` 用于复现验证
- 注意：GitHub 网络当前不通（schannel 断开），本次基于本地已有最新提交验证

---

## 一、环境搭建（E 盘，全部验证通过 ✅）

| 组件 | 位置 / 版本 | 状态 |
|---|---|---|
| Python venv | `E:\TrafficAgent-DSS\.venv`（Python 3.13.14，44 个包） | ✅ |
| SUMO 仿真器 | `E:\TrafficAgent-DSS\.sumo\sumo-1.21.0`（免安装解压版，sumo/sumo-gui 均可用） | ✅ `sumo --version` → 1.21.0 |
| TraCI/sumolib 绑定 | 1.21.0，与 SUMO 二进制同版（避免协议错配） | ✅ 冒烟导入通过 |
| LLM 密钥 | `.env` 中 DeepSeek 配置（base_url/model 已填），**API Key 仍为占位符** | ⚠️ 按用户选择先用规则模板模式（reasoning_mode=rule），拿到 Key 后写入 `.env` 的 `LLM_API_KEY` 即可 |
| 单元测试 | 113 passed / 59.3s（README 宣称 127 = 113 核心 + 14 E2E，E2E 需另装 chromium） | ✅ |

## 二、消融实验复跑（第 3 轮验证 ✅）

- 配置：5 种子（42/101/2024/777/999）× 600s × 事故窗口 150–420s，耗时 2m59s
- 结果文件：`experiments/ablation_rerun2_20260914.md`
- **与前两轮结果逐行完全一致**（跨进程、跨目录、跨 venv 三轮交叉比对均一致）——`zlib.crc32` 确定性修复确认有效

### 复跑核心数据（必须正视）

| 配置 | 延误(s/veh) | 最大排队(m) | 吞吐(veh/h) | CO₂(kg) |
|---|---|---|---|---|
| 无干预基线 | 29.14 ± 3.98 | 147.0 ± 58.7 | 5499.6 ± 269.4 | 314.4 ± 17.7 |
| 完整方案B | 28.80 ± 5.00 | 102.0 ± 65.5 | 5446.8 ± 326.5 | 320.3 ± 20.8 |
| 对照：仅 VMS | **26.98 ± 2.60** | 121.5 ± 50.7 | **5601.6 ± 222.7** | 309.6 ± 12.6 |

**结论：所有改善均未达统计显著（配对 t 检验 |t| 最大 1.91 < 2.776）；方案B 点估计不如"仅 VMS"；绿波为负贡献；CO₂ 反升 1.9%。** 噪声（种子间极差 9–13s）是效应量（−0.34s）的 26–38 倍。根因是场景标定与指标口径，不是代码 bug。

## 三、代码 / 答辩材料审查

完整报告（已存档，本次抽查验证全部属实）：
- `E:\TrafficAgent-DSS\docs\review_competition_20260914.md` —— 竞赛专业性与答辩风险审查（P0×4 / P1×2 / P2×1）
- `E:\TrafficAgent-DSS\docs\reproducibility_20260914.md` —— 从零复现验证报告

### 答辩前必须处理的 P0（按危险度排序）

1. **大屏默认数据是硬编码常量**（`src/web/app.py:158` `get_calibrated_rollout_data()`）——打开大屏第一屏就是写死的 44.6% 改善 / Level A+，被追问即穿帮
2. **存档消融报告与项目主张相反**——技术方案写"排队缩短 ≥30%"，实测 n=1 时排队 **+38.9%** 恶化；5 种子复跑也无一显著
3. **微观/中观两套引擎两套数字**——中观 −87% vs 微观 −4.7%，相差一个量级，同项目两套结论
4. **答辩材料数字对不上**：README"毫秒级推演"（实测秒~分钟级）、技术方案 ≥25%/≥30% 目标（实测未达成）

### 建议答辩主线（审查报告 §8）

把"真做过实验"作为卖点：LLM 数字溯源护栏、控制指令可核验、三轮可复现性证据——主动暴露问题并量化，比包装效果更好。

## 四、待办（用户后续决策）

- [ ] 拿到 DeepSeek API Key 后写入 `E:\TrafficAgent-DSS\.env` 的 `LLM_API_KEY`，然后跑一次端到端验证 LLM 模式
- [ ] 答辩前整改第一批（约 1 天）：大屏默认走真实推演、替换存档消融报告、统一 t 分布 CI、修正材料目标表述
- [ ] （可选）整改第二批：tripinfo 口径延误、κ 敏感性实验、绿波反馈车速闭环、走廊级排队指标
