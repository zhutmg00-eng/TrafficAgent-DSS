# TrafficAgent-DSS 竞赛专业性与答辩风险审查报告

- 日期：2026-09-14
- 审查对象：`zhutmg00-eng/TrafficAgent-DSS`（ITSAC 2026 参赛工程，HEAD `4d615a4` v2.2.1）
- 审查方式：全量源码走查 + 文档/实验产物交叉核对 + 已存档实验结果复算
- 审查视角：**评委视角的可信度 + 交通工程方法论严谨性 + 答辩现场可复现性**

---

## 0. 总体判断

**工程完成度高于同类参赛作品的平均值**——代码注释里保留了大量"踩坑—修复"记录（TraCI 相位破坏、`getArrivedNumber` 累积口径、hash 随机性、绿波权重重复施加等），说明确实真跑过仿真、真调过参数。LLM 只做叙事、不产数值，并配了"数字溯源护栏（numeric provenance guard）"，这是**非常有说服力的差异化亮点**，答辩应作为核心创新点来讲。

但存在 **3 个足以被评委当场击穿的问题**，且都集中在"数字从哪来、数字说了什么"：

| 等级 | 问题 | 一句话概括 |
|---|---|---|
| 🔴 P0 | 大屏默认数据仍是硬编码常量 | 答辩现场一开大屏，改善率就是写死的 44.6% |
| 🔴 P0 | 已存档消融实验**与项目主张相反** | 排队 **+38.9%**、延误仅 −4.7%、吞吐 0.0%（n=1，无统计意义） |
| 🔴 P0 | **本次 5 种子复跑：所有改善均不显著** | 延误 −0.34 s（t=−0.11）；绿波为负贡献；仅 VMS 反而最优 → 见 §2.2 |
| 🔴 P0 | 微观/中观两套引擎给出**两套不可比的数字** | 中观 −87%，微观 −4.7%，同项目两套结论 |
| 🟠 P1 | 延误口径、Webster 周期、绿波车速存在方法学硬伤 | 见 §3 |
| 🟠 P1 | 统计推断口径不一致（正态 z vs t 分布） | 见 §4 |
| 🟡 P2 | 实验设计与文档口径不一致（300s vs 600s） | 见 §5 |

---

## 1. 🔴 P0-1：Web 大屏默认仍服务硬编码"标定数据"

**证据**：`src/web/app.py:158-238` `get_calibrated_rollout_data()`

```python
b_delay = 46.8; b_queue = 105.0; b_speed = 22.4; ...
base_kpi = {"avg_delay_s": 84.5, "avg_speed_kmh": 9.4, ...}
```

算一下就明白严重性：

- 延误改善 = (84.5 − 46.8) / 84.5 = **44.6%**
- 车速提升 = (22.4 − 9.4) / 9.4 = **138.3%**
- 评级 = **卓越 (Level A+)**

这正是 9/12 那个 P0 提交里从"决策简报"中删掉的三个数字（commit message 原文：*删除 comparisons.strategy_b 及各项 KPI 的默认值（44.6%/138.3%/Level A+ 等）*）。**简报侧的假数据清了，但大屏侧的源头常量没清。**

**为什么危险**：`get_calibrated_rollout_data()` 是页面初始化默认数据源（`app.py:1114`）。评委/老师打开大屏看到的第一屏，就是这个 44.6% / Level A+。虽然有 `degradedNoticeBanner` 角标（`dashboard.js:486-512`），但**角标是灰字提示，数字是大号彩色卡片**——观感上就是"系统跑出了 44.6% 的改善"。若被追问"这个数是仿真跑出来的吗"，答案是否定的。

**修复建议（按优先级）**

1. **演示环境必须默认走真实推演**：把 `/api/rollout` 默认 `mode` 改为 `sumo`，标定数据改为显式勾选（`--allow-calibrated`）才可用；
2. 若必须保留快速模式，**不得输出"改善率/评级"**，只输出拓扑与结构，KPI 位显示 `—（未推演）`；
3. 前端把 `execution_mode` 提升为**红黄绿色徽章**置于指标区顶部，而非角落灰字。

---

## 2. 🔴 P0-2：已存档消融实验结论与项目主张相反

**证据**：`experiments/ablation_result_20260913_153022.md`（仓库里唯一的实测消融报告）

| 配置 | 延误(s/veh) | 最大排队(m) | 吞吐(veh/h) | CO2(kg) |
|---|---|---|---|---|
| 无干预基线 | 12.8 | 135.0 | 4476.0 | 93.94 |
| 方案A（仅 Webster） | 11.9 | 172.5 | 4320.0 | 92.56 |
| Webster + 绿波 | 14.0 | 105.0 | 4344.0 | 95.03 |
| **完整方案B** | **12.2** | **187.5** | **4476.0** | **93.22** |
| 仅 VMS | 12.1 | 150.0 | 4536.0 | 92.29 |

结论行原文：
```
- avg_delay_s: 降低 4.7%  (12.8 → 12.2)
- max_queue_m: 上升 38.9% (135.0 → 187.5)   ← 恶化
- throughput_vph: 降低 0.0%
- co2_emissions_kg: 降低 0.8%
```

**三处致命伤**

1. **排队恶化 38.9%** —— 而《技术方案》§4 写的目标是"最大排队缩短 ≥ 30%"。主张与实测直接冲突，评委只要翻到这份 md 就不攻自破；
2. **方案单调性断裂** —— 延误 A(11.9) < B(12.2)，"加绿波 + 加分流反而更差"，与"系统级协同优于单点"的核心论点相悖；
3. **n = 1** —— 报告表头 `随机种子: [42]`，所有 `±0.0`、无置信区间。单个种子的单次仿真**没有任何统计意义**，却以"真实 SUMO 实测"为标题呈现。

**注意**：这份报告用的是 `--duration 300 --incident 100-220`，与技术方案/默认参数的 **600s / 150-420s** 口径不一致，答辩材料里的数字对不上也是隐患。

**修复建议**

- 消融实验**至少 5 个种子**（`--seeds 42 101 2024 777 999`），统一 `duration=600 / incident=150-420`，重跑并替换这份报告；
- 若重跑后排队仍恶化，**如实写进报告并给出工程解释**（如：分流把车流从主线赶到长度更长的旁路，旁路排队未被 `J1_J2` 单车边指标捕获 → 说明指标口径需升级为"走廊总排队"或"路网总延误"）；这比藏起来更专业；
- 把"目标预期 ≥25%/≥30%"改成"实测结果 + 达成度分析"两段式，避免自设靶子打不中。

### 2.2 【本次复跑实测】5 种子 / 600s 口径 —— 结论：无任何指标达到统计显著

本机已装好 SUMO 1.21.0 并**完整复跑**消融实验（`experiments/ablation_result_20260914.md`，
原始数据 `ablation_raw_20260914_003637.json`），配置 5 种子 × 600s × 事故 150-420s：

| 配置 | 延误(s/veh) | 最大排队(m) | 吞吐(veh/h) | CO₂(kg) |
|---|---|---|---|---|
| 无干预基线 | 29.14 ± 3.98 | 147.0 ± 58.7 | 5499.6 ± 269.4 | 314.4 ± 17.7 |
| 方案A（仅 Webster） | 27.86 ± 3.88 | 112.5 ± 46.8 | 5590.8 ± 224.4 | 318.7 ± 19.7 |
| Webster + 绿波 | 30.36 ± 3.66 | **82.5** ± 79.7 | 5576.4 ± 226.4 | 326.8 ± 19.1 |
| **完整方案B** | **28.80 ± 5.00** | 102.0 ± 65.5 | 5446.8 ± 326.5 | 320.3 ± 20.8 |
| 对照：仅 VMS | **26.98 ± 2.60** | 121.5 ± 50.7 | **5601.6 ± 222.7** | 309.6 ± 12.6 |

**配对 t 检验（同种子配对，n=5，双尾 5% 临界值 |t| = 2.776）**

| 对比 | 配对差均值 | t 值 | 结论 |
|---|---|---|---|
| 基线 → 方案B 延误 | −0.34 s | −0.11 | **不显著**（逐种子 +9.4 / −8.0 / +2.8 / −4.3 / −1.6，正负交替） |
| 基线 → 方案B 排队 | −45.0 m | −1.91 | 不显著（方差极大，一个种子反而 +22.5） |
| 基线 → 方案B 吞吐 | −52.8 veh/h | −0.30 | 不显著且**方向为负** |
| 基线 → 方案B CO₂ | +5.86 kg | +0.64 | 不显著且**方向为负（增排）** |
| 基线 → 方案A 延误 | −1.28 s | −0.95 | 不显著 |
| 方案A → 加绿波 延误 | **+2.50 s** | +1.15 | **绿波使延误变差** |
| 基线 → 仅 VMS 延误 | −2.16 s | −1.29 | 不显著，但**点估计优于完整方案B** |

**三个必须正视的事实**

1. **没有任何一项改善达到统计显著**，逐种子差值正负交替，说明当前样本波动远大于治理效应——即"治理效果淹没在噪声里"；
2. **完整方案B 并不优于单一手段**：延误点估计 方案B(28.80) > 方案A(27.86) > 仅VMS(26.98)，"系统级协同最优"的核心论点**在当前标定下不成立**；
3. **绿波在当前参数下是负贡献**（+2.50 s/veh）。原因与 §3.3 一致：相位差按 50 km/h 自由流计算，而事故走廊实测车速只有 8~22 km/h，协调反而给主线加了不必要的约束。

> 这不是"系统不行"，而是**场景标定与评价指标还没调到位**。但从答辩角度，**这必须现在就知道**，而不是在评委提问时才发现。

**复现命令（本机已验证，约 3 分钟）**

```bash
export SUMO_HOME="E:\TrafficAgent-DSS\.sumo\sumo-1.21.0"
./.venv/Scripts/python.exe experiments/ablation.py \
  --seeds 42 101 2024 777 999 --duration 600 \
  --incident-start 150 --incident-end 420 \
  --out experiments/ablation_result_20260914.md
```

### 2.3 可复现性已验证（三轮交叉比对）

已按 README 流程做**干净 clone + 全新虚拟环境**的从零复现（详见 `docs/reproducibility_20260914.md`）：

| 轮次 | 环境 | 结果比对 |
|---|---|---|
| 主仓库首跑 | 本机 venv | 基准 |
| 主仓库复跑 | 同一 venv | ✅ 113 项 KPI 逐项完全相同 |
| **干净 clone + 全新 venv** | `E:\TrafficAgent-DSS-clean` | ✅ **跨目录逐项完全相同** |

`zlib.crc32` 替换内置 `hash()` 的确定性修复**确认有效**，这一条可以放心写进说明书的"可信度"章节。

**但要注意**："可复现"不等于"结论稳健"。同一配置换 seed 后延误极差达 9~13 s，而方案B 相对基线的效应量只有 **−0.34 s**——**噪声是效应的 26~38 倍**。这不是代码问题，是实验设计与场景标定问题（事故窗口内限速压到 0.5 m/s 使全网饱和，控制手段的边际作用被淹没；单场景单需求水平；瓶颈只看单边）。

---

## 3. 🟠 P1：交通工程方法学硬伤（评委最容易追问的技术点）

### 3.1 延误口径不是标准口径

`sumo_sandbox.py:539-549`：每 5 秒对**当前在网车辆**的 `getTimeLoss()` 求均值，再由 `evaluator.py:72` 对这些"瞬时均值"求时间平均。

两个偏差：

- **时间加权 ≠ 车辆加权**：拥堵时段在网车辆多，其样本被重复计入，平峰时段样本少，最终均值偏向拥堵时段，且与 tripinfo 全样本平均**不是同一个量**；
- 代码注释自己承认 tripinfo 有 977 辆车，而 KPI 只用 121 个采样点。

**建议**：直接启用 SUMO `--tripinfo-output`，聚合全部完成/在网车辆的 `timeLoss` 与 `duration` 做真实 trip 均值；或至少明确标注为"在网车辆瞬时平均时损（5s 采样）"，并与 tripinfo 结果做一次一致性对照（这是**加分项**：主动暴露口径差异并量化）。

### 3.2 实际下发的周期不是 Webster 最优周期

`traffic_agent.py:315`：

```python
design_cycle = min(120.0, max(60.0, round(timing["optimal_cycle"] * 2.0)))
```

按标定流量（1980/720 pcu/h）算，Webster 最优 C₀ ≈ 45 s，实际下发 **90 s**。过饱和加长周期在工程上有依据，但：

- 对外表述仍是"Webster 动态调优配时"，**名实不符**；
- 放大系数 2.0 **没有标定/敏感性实验支撑**，评委问"为什么是 2 倍"无法回答。

**建议**：① 改口径为"Webster 绿信比 + 过饱和周期放大（κ）"；② 做一组 κ ∈ {1.0, 1.5, 2.0, 2.5} 的敏感性实验，用数据选出 κ（**这本身就是很好的消融维度**）；③ 说明书里把 κ 写成可标定参数。

### 3.3 绿波相位差用的是自由流车速，与事故场景矛盾

`traffic_agent.py:332-337`：

```python
gw_plan = self.green_wave.compute_offsets(
    intersection_distances=[300.0, 300.0],   # 硬编码
    cycle_length=actual_cycle,
    progression_speed=13.89,                 # 50 km/h，硬编码
)
```

而同一份诊断输入里，瓶颈车速是 **8.2 km/h**。用 50 km/h 算出的相位差去协调一条 8 km/h 的拥堵走廊，物理上不成立；间距 300 m 也是常量，与 `corridor.net.xml` 未联动。

**建议**：相位差应基于**实测/仿真反馈的行程速度**迭代（可用上一轮仿真输出的瓶颈车速做二次修正，形成"推演—修正—再推演"的闭环，正好补足方案里缺失的 Reflexion Loop）；间距从 net.xml 解析。绿波带宽计算（`green_wave.py:10-34` 的圆环形交集法）实现得很好，但**带宽目前只展示、未进入评价与决策**，建议把"带宽比"纳入方案 B 的评价维度。

### 3.4 其他口径问题

| # | 问题 | 位置 |
|---|---|---|
| a | 排队只统计**单一瓶颈边** `J1_J2`，用 `halting × 7.5 m` 折算；分流后旁路排队不计入 → 可能正是 §2 排队恶化的原因 | `sumo_sandbox.py:423,524` |
| b | `delay_variance` 是"5s 采样的路网均值方差"，却被列为"运行平稳/可靠性"维度并设 ≥20% 目标 | `evaluator.py:19-24` |
| c | 双向绿波权重 `w=0.6` 缺依据；`w=0.5` 才是双向等权经典解 | `green_wave.py:60` |
| d | 评级函数把"卓越 A+"门槛设在延误≥25% 且排队≥25%，属**自设阈值**，应改为相对基线的统计显著性判定 | `evaluator.py:152-155` |

---

## 4. 🟠 P1：统计推断口径不一致

- `traffic_agent.py:1099-1101`：`run_multi_seed_evaluation()` 用 **1.96（正态 z）**算 95% CI；
- `experiments/ablation.py:61-63`：用 **t 分布**临界值表。

n = 5 时 t(0.975,4) = 2.776，与 1.96 相差 42%，**正态近似在小样本下明显偏乐观**。同一项目两套 CI 口径，答辩被追问即暴露。

**建议**

1. 统一到 t 分布（或改为 bootstrap BCa）；
2. 三方案用**同一随机种子**运行，本质是**配对设计**，应做**配对 t 检验 / Wilcoxon**，而不是比较两组独立 CI 是否重叠——配对检验的功效高得多，也更能支撑"显著改善"的结论；
3. 报告补充**效应量**（Cohen's d）与置信区间，而非只报 p 值或"显著性布尔值"。

---

## 5. 🟡 P2：实验设计与文档一致性

| # | 问题 | 说明 |
|---|---|---|
| 1 | 消融实验 5 配置 × 每 seed 跑 3 次完整仿真（baseline/A/B 每次都重跑） | 15 次/seed，基线被重复计算 5 次，浪费算力；可缓存基线 |
| 2 | 存档报告 300s / 事故 100-220s，文档与默认 600s / 150-420s | 材料数字对不上 |
| 3 | 无"无事故"对照组 | 无法说明方案在正常高峰下是否会**反向恶化**（这是评委高频提问） |
| 4 | 无需求敏感性分析（V/C = 0.7/0.9/1.05） | 无法界定方案适用边界 |
| 5 | `requirements.txt` 无版本锁、无 SUMO 版本声明 | 换机复现门槛高（本次复现已验证：SUMO 需 200MB+ 单独下载） |
| 6 | 微观引擎走 `corridor.*`（抽象走廊，3 路口），中观引擎走 `network_xizhimen.json`（591 节点真实路网） | **两张皮**：两套路网、两套指标，不可比 |

**第 6 条要重点讲**：`docs/review_findings.md:90` 记载中观引擎结果为"延误 −87%、排队 −71%、速度 +68%"，而微观 SUMO 实测是 −4.7% / +38.9%。同一项目里，真实路网的中观引擎给出 −87%，抽象走廊的微观引擎给出 −4.7%，**两者相差一个量级**。如果答辩时两张图都被看到，会产生"哪个是真的"的致命质疑。

**建议**：做一次**同场景中观 vs 微观的一致性校验**（同一 O-D、同一配时、同一事故窗口），报告中给出两者的偏差范围与各自适用边界；或直接统一为一条链路（推荐：**微观 SUMO 出结论，中观只做实时可视化底图**）。

---

## 6. 答辩表述（材料 vs 事实）核对

| 材料表述 | 事实 | 建议 |
|---|---|---|
| README：Tests 127 Passed（113 Core + 14 E2E） | ✅ 属实（实测：24+10+46+33=113，E2E 14） | 可放心使用 |
| README："毫秒级事前 What-If 推演" | 实测单次 600s 推演约需数秒~数十秒（3 方案串行） | 改为"秒级~十秒级"或给出实测耗时表 |
| 技术方案 §2："路网为 J1–J3 走廊标定路网，尚未接入 OSM" | ✅ 已诚实注明（但 README 顶部仍有"591 节点真实路网"的表述，属中观链路） | 明确区分"微观走廊 / 中观真实路网"两条链路 |
| 技术方案 §4：延误降低 ≥25%、排队缩短 ≥30% | ❌ 实测 −4.7% / +38.9% | 删除或改为"改善方向性目标"，用实测值替代 |
| "数字孪生" | 抽象走廊 + 真实路网混用 | 统一概念：**微观沙盒（SUMO，走廊）+ 中观底图（真实路网）** |

---

## 7. 建议的整改顺序（含工作量估计）

**第一批（答辩前必须完成，约 1 天）**

1. 把大屏默认数据源切换为真实推演，屏蔽标定常量中的改善率/评级（§1）；
2. 用 5 个种子、统一 600s/150-420s 重跑消融实验，替换存档报告（§2）；
3. 统一 CI 口径为 t 分布 + 配对检验（§4）；
4. 修正说明书中的目标预期表述（§6）。

**第二批（显著提升专业度，约 2-3 天）**

5. 延误指标改用 tripinfo 全样本口径，并做一次"口径差异量化"对照（§3.1，加分项）；
6. κ 敏感性实验（§3.2）；
7. 绿波相位差改用反馈车速 + 从 net.xml 解析间距，形成"推演—修正"迭代闭环（§3.3）；
8. 排队/延误指标扩展为走廊级总量，覆盖旁路（§3.4-a）。

**第三批（锦上添花）**

9. 中观 vs 微观一致性校验（§5-6）；
10. 增加"无事故对照"与 V/C 敏感性实验（§5-3/4）；
11. 锁定依赖版本 + 写清 SUMO 版本要求（§5-5）。

---

## 8. 答辩时应该主动讲的三个"亮点"（这些都是真的）

1. **LLM 防幻觉护栏**：`_narrative_numbers_traceable()`（traffic_agent.py:196-268）会校验模型叙事里出现的每一个数字，只要不能溯源到工具输出，就整体拒绝并降级。这在参赛作品里很少见，是硬核卖点。
2. **控制指令可核验（Control Evidence）**：每次推演都记录"实际下发了多少条信号指令、多少条重路由指令、事故车道是否真的被改速又还原"，杜绝"声称做了但没做"。
3. **踩坑即文档**：代码里保留了十余处"曾经的错误实现 + 为什么错 + 正确做法"（如绿波权重重复施加导致 J3 相位差 80.6 s、setPhaseDuration 破坏感应控制）。这证明是**真做过实验**，而非包装。

**答辩策略建议**：把"我们发现了哪些问题、怎么修的、修完数字怎么变"讲成主线，比"我们的系统多厉害"更可信——评委对参赛作品的默认预期是"有水分"，主动暴露并量化问题反而建立信任。
